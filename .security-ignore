# Security Vulnerability Ignore List
# Purpose: Document accepted vulnerabilities for dev dependencies
# Scope: Development-only dependencies with acceptable risk
#
# Each line is a vulnerability ID (GHSA-* or CVE-*) to ignore.
# Add comments above each ID explaining why it's acceptable.

# pytest: dev/test-only runner. CVE-2025-71176 is fixed in pytest 9.0.3, a major
# upgrade gated by pytest-asyncio/pytest-cov compatibility and not yet adopted.
# Risk: test runner only - never shipped in the package nor run against untrusted
# input. Remove this entry when the test stack moves to pytest >= 9.0.3.
GHSA-6w46-j5rx-g56g
