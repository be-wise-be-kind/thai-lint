"""
Purpose: Test module initialization for Docker integration tests

Scope: Docker container tests for thai-lint CLI

Overview: Provides test package initialization for Docker integration testing, which verifies
    that the Docker image builds correctly, runs the CLI properly, supports volume mounting,
    and executes linting operations correctly in a containerized environment.

Dependencies: pytest, Docker SDK for Python (if available)

Exports: Test package for Docker functionality

Interfaces: Test module imports for pytest discovery

Implementation: Standard Python package initialization for test discovery
"""
