"""
Purpose: Test package for lazy-ignores linter unit tests

Scope: All unit tests for lazy-ignores pattern detection and validation

Overview: Package initialization for lazy-ignores linter tests. Contains tests for Python
    ignore detection (noqa, type:ignore, pylint, nosec), TypeScript ignore detection
    (ts-ignore, eslint-disable), thai-lint ignore detection (thailint:ignore variants),
    header suppression parsing, orphaned entry detection, and violation message formatting.

Dependencies: pytest, src.linters.lazy_ignores

Exports: Test modules for lazy-ignores linter

Interfaces: pytest test discovery

Implementation: Package initialization for test organization
"""
